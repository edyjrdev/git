ssh-keygen -t ed25519 -C "edyjrdev@gmail.com"

/user/.ssh/id_ed25519.pub

adicionar SSH no git

git config --global user.name "Edy Epumuceno Rodrigues Junior"
git config --global user.email "edyjrdev@gmail.com"

git config --list --global

git clone git@github.com:edyjrdev/git.git

git status

git add .

git commit . -m "comentario de commit"

git push

git pull


#adicionar remoto
git branch -M main
git remote add origin git@github.com:edyjrdev/python3guide.git
git push -u origin main

# apagar repositorio de uma pasta
sudo rm -r .git
